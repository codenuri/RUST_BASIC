fn main() 
{
    let t1 = (1, "hi", 3.5);
	let t2 = (1, "hi", 3.5);

	println!("t1 == t2 ? {}", t1 == t2);
    println!("len = {}", 	  t1.len());
    println!("is_empty = {}", ().is_empty());
}
